# ctfcli challenge file
name: "web sample1"
author: "god_of_ctf"
category: "web"
description: |
  A regex case sensitive flag

# test this flag
flags:
  - { type: "regex", content: "flag{.*LOVE_CTF.*}" }
  - { type: "regex", content: "flag{.*LIKE_CTF.*}" }
type: static